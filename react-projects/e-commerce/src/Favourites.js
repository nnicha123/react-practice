import React, { Component } from 'react'

class Favourites extends Component {
    render() {
        return (
            <div>
                {this.props.title}
            </div>
        )
    }
}

export default Favourites
